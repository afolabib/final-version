# freemi.ai Build Board

## Operating Rule
Every 5 minutes:
1. pick the next unfinished task
2. execute one bounded step
3. run audit (build, route, runtime, data/regression as applicable)
4. if blocked, mark blocked with blocker note and move to next task on next run
5. append progress log

## Tasks
- [x] T1 Replace AuthContext fully
- [ ] T2 Remove Base44 app boot dependency
- [ ] T3 Dashboard shell integrity pass
- [x] T4 Lock Wizard.jsx as canonical setup flow
- [x] T5 Remove checkout from active wizard path
- [x] T6 Firestore onboarding persistence
- [ ] T7 Direct deploy frontend handoff
- [x] T8 Refactor provisionInstance into real deployment orchestrator
- [ ] T9 Add instance event logging
- [ ] T10 Generate deployment profile from wizard answers
- [ ] T11 Generate OpenClaw workspace files
- [ ] T12 Seed workspace into runtime volume
- [ ] T13 Health-check activation pipeline
- [ ] T14 Replace InstancesView fully
- [ ] T15 Add manage/start/stop/restart/delete actions
- [ ] T16 Real provisioning state in wizard
- [ ] T17 Remove old Base44 onboarding flow
- [ ] T18 Remove Base44 client/package dependency
- [ ] T19 Deferred cleanup wave

## Progress Log
- Initialized build board.
- 2026-03-27 16:13 PDT — T1 bounded step: replaced `src/lib/AuthContext.jsx` Base44 boot/auth loop with Firebase auth-state provider and switched dashboard sign-out to context logout. Audit: `npm run build` ✅ (exit 0). Remaining: remove Base44 auth assumptions from protected/admin flows and route guards.
- 2026-03-27 16:17 PDT — T1 bounded step: removed `PageNotFound` dependency on `base44.auth.me()` and wired it to shared Firebase auth context. Audit: `npm run build` ✅ (exit 0). Remaining: admin authorization still needs a Firebase-native role/claims source.
- 2026-03-27 16:22 PDT — T1 bounded step: added Firebase-native admin resolution via `VITE_ADMIN_EMAILS` allowlist in `AuthContext` and switched `ProtectedAdminRoute` to use `user.isAdmin`. Audit: `npm run build` ✅ (exit 0). Remaining: replace email allowlist with server-issued custom claims for production-hard admin auth.
- 2026-03-27 16:29 PDT — T2 bounded step: removed the Base44 Vite boot plugin from `vite.config.js` and replaced it with a plain Vite config using an explicit `@ -> src` alias, so app boot no longer depends on Base44-specific plugin wiring. Audit: `npm run build` ✅ (exit 0). Blockers: none. Remaining: remove Base44 runtime URL/app-param assumptions and migrate the remaining `base44` client calls off the frontend boot path.
- 2026-03-27 16:37 PDT — T8 bounded step: wired the dashboard wizard’s post-checkout path to resume the saved onboarding session and call the Firebase `provisionInstance` callable directly, and expanded `functions/src/provisionInstance.ts` to persist onboarding/provisioning metadata for Fly.io OpenClaw deployments. Audit: `npm run build` ✅ in app, `npm run build` ✅ in `functions/`. Blockers: none. Remaining: add idempotency/duplicate-provision guards and feed generated deployment/workspace profiles into the callable before machine creation.
- 2026-03-27 16:39 PDT — T8 bounded step: added an idempotent reuse path to `functions/src/provisionInstance.ts` so repeat post-checkout calls for the same onboarding session return the existing Firestore instance record instead of creating another Fly machine, and new instances now persist `onboardingSessionId` for traceability. Audit: `npm run build` ✅ in app, `npm run build` ✅ in `functions/`. Blockers: none. Remaining: prevent in-flight duplicate machine creation before the first instance record exists, then feed generated deployment/workspace profiles into the callable before machine creation.
- 2026-03-27 16:45 PDT — T3 bounded step: replaced the dashboard sidebar’s lingering Base44 `Agent.list()` dependency with a Firebase/Firestore query against the `instances` collection, so the dashboard shell no longer throws on sidebar boot during the cutover and deployed entries now route to `/dashboard/instances`. Audit: `npm run build` ✅ in app. Blockers: none. Remaining: `AgentsView` and `InstancesView` still depend on Base44 APIs, so the rest of the dashboard shell needs the same Firebase-native pass.
- 2026-03-27 16:58 PDT — T10 bounded step: added deployment-profile generation inside `functions/src/provisionInstance.ts`, deriving a canonical Fly.io/OpenClaw deployment profile from wizard answers before machine creation and persisting that profile onto both `instances` and `onboarding_sessions`. The profile now captures target metadata, runtime env, and workspace seed hints for the next direct-provisioning steps. Audit: `npm run build` ✅ in app, `npm run build` ✅ in `functions/`. Blockers: none. Remaining: T11 should generate concrete OpenClaw workspace files from this profile so the volume can be seeded before/at machine boot.
- 2026-03-27 18:05 PDT — T11 bounded step: persisted the generated OpenClaw workspace bundle from `functions/src/openclawWorkspace.ts` onto both `instances` and `onboarding_sessions`, and included it in the callable response/reuse path so downstream provisioning can seed the Fly volume from concrete files instead of just profile hints. Audit: `npm run build` ✅ in app, `npm run build` ✅ in `functions/`. Blockers: none. Remaining: T12 should write this stored workspace bundle into the runtime volume during/after machine creation.
- 2026-03-27 19:08 PDT — T12 bounded step: added post-boot Fly machine workspace seeding in `functions/src/provisionInstance.ts` using the Machines API `/exec` endpoint, so the generated OpenClaw workspace bundle is now written into `/home/node/.openclaw/workspace` on the mounted volume immediately after machine start, with seed metadata/results persisted back to Firestore and onboarding session state. Audit: `npm run build` ✅ in app, `npm run build` ✅ in `functions/`. Blockers: none in static audit; runtime validation against a live Fly machine is still needed. Remaining: T13 should hook health-check activation onto the seeded workspace/runtime so successful provisioning transitions from seeded to verified healthy.
- 2026-03-27 20:08 PDT — T13 bounded step: added post-provision health verification in `functions/src/provisionInstance.ts`, polling `${deploymentUrl}/health` after workspace seeding and persisting `healthCheck` attempts/results plus healthy vs unverified status onto both `instances` and `onboarding_sessions`. Reuse responses now also return stored health-check metadata. Audit: `npm run build` ✅ in app, `npm run build` ✅ in `functions/`. Blockers: none in static audit; this still needs live Fly runtime validation to confirm the OpenClaw image exposes a healthy `/health` response on first boot. Remaining: harden the activation pipeline with runtime validation and follow-up event logging/state transitions once live checks are confirmed.
- 2026-03-27 21:13 PDT — T9 bounded step: added provisioning event logging in `functions/src/provisionInstance.ts`, writing structured lifecycle events (`provisioning_started`, profile generation, machine creation, workspace seeding, health-check completion, reuse, failure, and delete) into Firestore `events` subcollections for onboarding sessions and instances. Audit: `npm run build` ✅ in app, `npm run build` ✅ in `functions/`. Blockers: none in static audit; event logs are now persisted server-side but not yet surfaced in the dashboard UI or validated against a live Fly provisioning run. Remaining: wire these event logs into the Firebase-native instances/dashboard flow so operators can see real provisioning state without Base44.
- 2026-03-27 22:17 PDT — T1 bounded step: added `src/components/ProtectedRoute.jsx` and restructured `src/App.jsx` so Firebase-authenticated dashboard routes now sit behind a real route guard while `/dashboard/wizard` and `/dashboard/picker` remain publicly reachable for onboarding. This removes the last degraded-mode auth gap where signed-out users could still load internal dashboard screens during the Base44→Firebase cutover. Audit: `npm run build` ✅ in app (static build passed; `dist/` regenerated). Blockers: none. Remaining: replace the temporary admin email allowlist with server-issued custom claims so admin auth is Firebase-native end-to-end.
- 2026-03-27 23:18 PDT — T4 bounded step: locked `src/pages/dashboard/Wizard.jsx` in as the canonical onboarding flow by rewiring `src/components/dashboard/AgentPicker.jsx` to navigate directly into `/dashboard/wizard` with the selected template, and teaching `Wizard.jsx` to hydrate that routed agent selection immediately into the Firebase/Stripe/Fly.io flow. This removes the active dependency on the legacy modal checkout path for picker-driven deployments, so new onboarding traffic now lands on the cutover-safe wizard instead of the old Base44 flow. Audit: `npm run build` ✅ in app; regression spot-check `grep -R "<WizardModal\|from './WizardModal'" src` ✅ (no remaining imports/usages). Blockers: none. Remaining: finish T4 by retiring the now-orphaned `WizardModal` / `PaymentStep` / `OnboardingWizard` legacy components and any stale route copy so there is only one setup surface left in the codebase.
- 2026-03-28 00:21 PDT — T4 bounded step: retired the dead legacy onboarding surface by removing `src/components/dashboard/AgentSetupWizard.jsx`, `src/components/dashboard/WizardModal.jsx`, `src/components/dashboard/OnboardingWizard.jsx`, `src/components/dashboard/PaymentStep.jsx`, and the unused legacy `src/pages/Dashboard.jsx` shell. `src/pages/dashboard/Wizard.jsx` is now the only setup flow left in the codebase, which closes the stale Base44-era onboarding branch and keeps the Firebase/Fly.io cutover path canonical. Audit: `npm run build` ✅ in app; legacy surface regression grep for `AgentSetupWizard|WizardModal|OnboardingWizard|PaymentStep` in `src/` ✅ (no remaining references). Blockers: none. Remaining: move to T5 and remove checkout from the active wizard path so provisioning can advance directly into the OpenClaw/Fly.io flow.
- 2026-03-28 01:22 PDT — T5 bounded step: removed Stripe checkout from the active `src/pages/dashboard/Wizard.jsx` flow. The wizard now creates the onboarding session, transitions straight into the provisioning step, and calls the Firebase `provisionInstance` callable directly instead of redirecting through `createCheckoutSession` / `?checkout=success` resume params. UI copy and CTA text were updated to reflect direct Fly.io/OpenClaw deployment, and the provisioning handoff panel now shows a direct provisioning payload instead of checkout state. Audit: `npm run build` ✅ in app; wizard regression grep for `createCheckoutSession|checkout=|Continue to checkout|Preparing checkout|pending_checkout_success|post_checkout_resume|checkoutState` in `src/pages/dashboard/Wizard.jsx` ✅ (no remaining active checkout-path references). Blockers: none for this bounded step. Remaining: move to T6 so the direct wizard path persists and rehydrates onboarding state from Firestore cleanly through the provisioning lifecycle.
- 2026-03-28 01:52 PDT — T6 bounded step: replaced the old modal wizard’s Base44 onboarding save with Firebase/Firestore onboarding sessions in `src/components/dashboard/PaymentStep.jsx`, preserving the modal UX while skipping Stripe and storing normalized agent answers against the canonical agent template/question schema. Also fixed `src/components/dashboard/WizardModal.jsx` so typed custom answers are committed before advancing, preventing freeform responses from being dropped before the Firestore save. Audit: `npm run build` ✅ in app; regression checks ✅ `PaymentStep.jsx` no longer references `base44.entities.OnboardingData.create`, now imports/calls `createOnboardingSession`, and `WizardModal.jsx` contains the new `commitCurrentAnswer` path. Blockers: none for this bounded step. Remaining: continue T6 by rehydrating/resuming the saved onboarding session through the modal loading/provisioning path, then wire the modal directly into `provisionInstance`.
- 2026-03-28 01:51 PDT — T7 bounded step: turned the old modal wizard’s fake loading screen into a real Firestore-backed deploy handoff. `src/components/dashboard/SetupLoadingStep.jsx` now rehydrates the saved onboarding session by id and calls the Firebase `provisionInstance` callable under the existing modal UX, while `src/lib/onboardingService.js` can fetch a specific onboarding session from Firestore (with local fallback) before provisioning. Audit: `npm run build` ✅ in app; regression grep ✅ `SetupLoadingStep.jsx` now imports/calls `getOnboardingSession` + `provisionInstance`, and `onboardingService.js` now supports `getOnboardingSession(sessionId)`. Blockers: none for this bounded step. Remaining: keep T7 open to surface richer live provisioning status in the modal/success state, then move to T8 (`provisionInstance` onboarding-session cleanup).
- 2026-03-28 02:00 PDT — T8 bounded step: made `functions/src/provisionInstance.ts` consume onboarding sessions as the canonical source of truth when a session id is present. The callable now resolves and validates the Firestore onboarding session up front, falls back to its stored `plan` / `agentType` / `answers` when callers omit them, logs whether answers came from the request or the saved session, and persists the resolved values onto the generated deployment profile and instance record. Audit: `npm run build` ✅ in app; `npm run build` ✅ in `functions/`; regression grep ✅ `provisionInstance.ts` now contains `resolveOnboardingSessionContext` plus the resolved `effectivePlan` / `effectiveAgentType` / `effectiveAnswers` flow. Blockers: none for this bounded step. Remaining: harden T8 by handling in-flight duplicate provisioning before `instanceId` exists on the onboarding session, then move to T16/T1 follow-ups.
- 2026-03-28 02:12 PDT — T7 bounded step: upgraded the old modal wizard’s `SetupLoadingStep` from a fire-and-forget handoff into a Firestore-backed live status surface. It now subscribes to the saved `onboarding_sessions/{id}` document, reflects real backend states (`provisioning_started`, `provisioned_unverified`, `healthy`, `provisioning_failed`) inside the classic modal UX, and only advances once the backend has actually moved the session forward. Audit: `npm run build` ✅ in app; regression grep ✅ `SetupLoadingStep.jsx` now imports/calls `subscribeToOnboardingSession`, reacts to `sessionStatus === 'healthy'` / `'provisioning_started'`, and `onboardingService.js` now exports the realtime subscription helper. Blockers: none for this bounded step. Remaining: T7 still needs the success view to consume the same real deployment data instead of hard-coded “agent is live” copy, then the next preferred backend task is T8 duplicate/in-flight provisioning hardening.
- 2026-03-28 02:23 PDT — T7 bounded step: wired the old modal wizard’s success state to the real deployment handoff instead of static “agent is live” copy. `SetupLoadingStep.jsx` now passes the live onboarding/deployment context forward, `WizardModal.jsx` preserves the selected channel plus deployment result through the modal phases, and `DeploySuccessStep.jsx` now renders the real instance id / deployment URL / backend status with CTAs that open the actual runtime when available. Audit: `npm run build` ✅ in app; regression grep ✅ `WizardModal.jsx` now carries `deployConfig` + `deploymentResult`, `SetupLoadingStep.jsx` calls `onComplete(deploymentContext)`, and `DeploySuccessStep.jsx` consumes `deploymentUrl` / `instanceId` / `status`. Blockers: none for this bounded step. Remaining: T7 still has channel-specific completion work (for example, wiring Telegram/WhatsApp-specific post-deploy instructions off real backend state), then the next preferred backend task is T8 duplicate/in-flight provisioning hardening.
- 2026-03-28 03:31 PDT — T8 bounded step: hardened `functions/src/provisionInstance.ts` against duplicate in-flight provisioning from the old modal flow by adding a Firestore transaction claim on `onboarding_sessions/{id}`. A fresh `provisioningLockToken` now gates machine creation before any `instanceId` exists, concurrent/retry calls reuse the in-flight session instead of creating a second Fly machine, and the lock is cleared on reuse completion, success, and failure. To preserve the old modal UX, `src/components/dashboard/SetupLoadingStep.jsx` now treats callable success as “ready” only when a real provisioned result exists, so an in-flight reuse response stays on the live loading state until Firestore status advances. Audit: `npm run build` ✅ in app; `npm run build` ✅ in `functions/`; regression grep ✅ `SetupLoadingStep.jsx` contains `hasProvisionedResult`, and `provisionInstance.ts` contains `claimProvisioningForSession`, `provisioning_reused_inflight`, and `provisioningLockToken` cleanup paths. Blockers: none for this bounded step. Remaining: move to T16 channel/runtime-state polish in the modal or finish the remaining T1 Firebase auth cutover follow-up.
- 2026-03-28 04:36 PDT — T1 bounded step: moved admin-role resolution in `src/lib/AuthContext.jsx` onto Firebase token claims instead of the old frontend-only Base44-era auth assumptions. The auth provider now listens with `onIdTokenChanged`, hydrates `claims` via `getIdTokenResult`, marks admin users from Firebase custom claims (`admin`, `isAdmin`, or `role=admin`), and exposes `refreshUserClaims()` so the dashboard can pick up server-issued role changes without a full re-login. A legacy `VITE_ADMIN_EMAILS` fallback remains only as a temporary escape hatch and is tagged as `adminSource: 'legacy-email-allowlist'` so the final cleanup is obvious. Audit: `npm run build` ✅ in app; regression grep ✅ `AuthContext.jsx` now contains `getIdTokenResult`, `onIdTokenChanged`, `adminSource`, and `refreshUserClaims`. Blockers: none for this bounded step. Remaining: issue/administer Firebase custom claims server-side and then remove the temporary email allowlist fallback to finish T1 end-to-end.
- 2026-03-28 05:39 PDT — T7 bounded step: upgraded the old modal wizard’s success state to stay channel-aware after the direct Firebase/Fly.io deploy handoff. `src/components/dashboard/WizardModal.jsx` now carries the full `deployConfig` into `DeploySuccessStep.jsx`, and the success UI renders real channel-specific next steps for web, WhatsApp, and Telegram using the live deployment status/URL instead of generic copy. This keeps the classic modal UX intact while making the direct deploy path clearer after provisioning starts or goes healthy. Audit: `npm run build` ✅ in app; regression grep ✅ `WizardModal.jsx` now passes `deployConfig`, and `DeploySuccessStep.jsx` now contains `buildChannelInstructions` plus the new WhatsApp/Telegram/Web completion cards. Blockers: none for this bounded step. Remaining: T7 still needs actual channel activation/runtime wiring behind those post-deploy instructions, then move to T16/T1 follow-ups.
- 2026-03-28 06:42 PDT — T7 bounded step: persisted the old modal wizard’s chosen deploy channel into Firestore onboarding sessions and fed that saved deployment target back into the backend deployment profile. `src/lib/onboardingService.js` now stores a sanitized `deploymentConfig` plus `deploymentTarget`, `src/components/dashboard/PaymentStep.jsx` and `src/components/dashboard/WizardModal.jsx` carry the modal’s `deployConfig` through the Firestore save, `src/pages/dashboard/Wizard.jsx` stamps the canonical web wizard path as `web`, and `functions/src/provisionInstance.ts` now falls back to the saved `deploymentTarget` when deriving the runtime communication channel. Audit: `npm run build` ✅ in app; `npm run build` ✅ in `functions/`; regression grep ✅ confirmed `deploymentConfig`/`deploymentTarget` persistence in the wizard save path and backend profile fallback in `provisionInstance.ts`. Blockers: none for this bounded step. Remaining: T7 still needs actual runtime/channel activation wiring (especially Telegram/WhatsApp) beyond storing the selected target, then move to T16/T1 follow-ups.
- 2026-03-28 07:45 PDT — T1 bounded step: finished the AuthContext Firebase cutover by removing the last frontend-only `VITE_ADMIN_EMAILS` / `legacy-email-allowlist` admin fallback from `src/lib/AuthContext.jsx`. Admin access is now derived strictly from Firebase custom claims (`admin`, `isAdmin`, or `role=admin`), which matches the existing `functions/src/adminClaims.ts` server-side claim issuer and closes the last Base44-era auth escape hatch in the route guard path. Audit: `npm run build` ✅ in app; regression grep ✅ confirmed `AuthContext.jsx` no longer contains `VITE_ADMIN_EMAILS` or `legacy-email-allowlist`, and no legacy allowlist references remain under `src/` or `functions/src/`. Blockers: none for this bounded step. Remaining: T7 channel/runtime activation polish and T16 real provisioning state remain the highest-value next modal-flow tasks.
