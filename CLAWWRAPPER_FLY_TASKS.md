# ClawWrapper → Fly.io → OpenClaw Launch Path Task List

## Goal
Keep the existing modal wizard and Firebase onboarding flow, but replace freemi.ai's failing Fly/OpenClaw launch path with a ClawWrapper-style thin deployment path that results in a publicly reachable runtime.

## Rules
- Keep the modal wizard intact.
- Do not redesign the frontend flow.
- Focus on one concrete task at a time.
- After each task: build, validate, report.
- If blocked: note the exact blocker and move to the next unblock path.

## Ordered Tasks
- [x] T1 Freeze modal flow as canonical and stop further frontend churn.
- [x] T2 Extract exact ClawWrapper Fly/OpenClaw launch assumptions from code.
- [x] T3 Diff freemi.ai deploy engine vs ClawWrapper deploy engine.
- [x] T4 Thin `functions/src/provisionInstance.ts` so launch path only does: onboarding resolve → env build → volume create → machine create → state persist.
- [x] T5 Create dedicated `containerEnv` helper mirroring ClawWrapper's thin env builder.
- [x] T6 Replace public URL assumptions with app-level Fly hostname strategy.
- [x] T7 Determine why stock image launch path forces loopback and document exact failing behavior.
- [x] T8 Implement custom Fly/OpenClaw startup path modeled on ClawWrapper-compatible launch assumptions.
- [x] T9 Create fresh validation machine on Fly using the new launch path.
- [x] T10 Validate external reachability via `https://maagic-operators.fly.dev/health`.
- [x] T11 Reintegrate the working launch path into the real modal deploy flow.
- [ ] T12 Verify end-to-end: modal wizard → Firestore → Fly machine → public health → dashboard state.

## Validation Checklist
- frontend build passes
- functions build passes
- machine creates successfully
- machine starts successfully
- OpenClaw responds locally
- OpenClaw responds publicly
- dashboard shows correct deployment state

## Progress Log
- Initialized ClawWrapper/Fly/OpenClaw task list.
- 2026-03-29 16:29 PDT — T1 bounded step: froze the existing modal wizard as the canonical onboarding surface. `src/pages/dashboard/Wizard.jsx` is now only a compatibility bridge that redirects back into `/dashboard/picker`, and `src/components/dashboard/AgentPicker.jsx` can reopen `WizardModal` from route state so older deep links still land in the same modal flow instead of a second divergent wizard UI. Validation: `npm run build` ✅ (app, exit 0).
- 2026-03-29 16:40 PDT — T2 bounded step: extracted the live ClawWrapper launch assumptions from code and compared them against freemi.ai’s current Firebase launcher. ClawWrapper’s concrete assumptions are: (1) launch path is thin and bounded to `createVolume` → `buildMachineConfig` → `createMachine` → `wait started` (`ClawWrapper/lib/fly/volumes.ts`, `lib/fly/config.ts`, `lib/fly/machines.ts`); (2) the runtime must bind publicly on Fly by forcing `HOST=0.0.0.0` and, for OpenClaw, relying on a container startup path compatible with LAN binding (`docs/DEPLOYMENT.md`, `lib/fly/config.ts`); (3) service routing is app-level Fly proxying on ports 80/443 to a single `internal_port` with `/health` HTTP checks and no per-user public URL rewrite in machine creation (`lib/fly/config.ts`, `lib/fly/machines.ts`); (4) persistent state is only the mounted volume at `/home/node/.openclaw/workspace`; and (5) any customer-specific config beyond env is handled outside the initial machine-create path. In contrast, freemi.ai’s current `functions/src/provisionInstance.ts` still hardcodes `AGENT_URL` to the shared app hostname, injects OpenClaw-specific command/env assumptions inline, and stores a generated workspace bundle in Firestore without actually making machine config construction a reusable ClawWrapper-style helper yet. Validation: task audit/read-through ✅; no code-path changes beyond updating this tracker, so build state remains unchanged from prior green baseline.
- 2026-03-29 16:47 PDT — T3 bounded step: completed a code-level diff of freemi.ai’s deploy engine versus ClawWrapper’s deploy engine and pinned the exact deltas the next refactor must remove. Diff summary: (1) ClawWrapper splits responsibilities cleanly across `createVolume`, `buildMachineConfig`, and `createMachine`, while freemi.ai still concentrates Fly API calls, machine config assembly, and Firestore side effects inside `functions/src/provisionInstance.ts`; (2) ClawWrapper treats machine env as a thin input merged into a reusable config builder, while freemi.ai mixes generic env (`HOST`) with OpenClaw launch assumptions (`cmd`, `OPENCLAW_GATEWAY_BIND`, `AGENT_URL`, metadata) inline inside `createFlyMachine`; (3) ClawWrapper’s machine create path returns only infra identifiers plus the shared Fly hostname, while freemi.ai also generates/stores a deployment profile, generated workspace bundle, health placeholder, and onboarding lock state in the same execution path; (4) ClawWrapper has explicit cleanup of orphaned volumes on machine-create failure, but freemi.ai’s `createFlyMachine` currently leaves volume cleanup to outer delete flows and has no local rollback if machine creation fails after volume creation; and (5) ClawWrapper’s config abstraction already matches the target thin-launch model, which means freemi.ai’s highest-leverage next step is to extract env/config helpers and make `provisionInstance` orchestrate rather than construct the Fly machine shape directly. Validation target set from this diff: preserve current Firestore persistence semantics, but move launch-path construction toward helperized `resolve onboarding → build env → create volume → build config → create machine → persist state` parity. Validation: tracker update complete; implementation untouched pending T4/T5.
- 2026-03-29 16:57 PDT — T4 bounded step: thinned `functions/src/provisionInstance.ts` so the provisioning flow now orchestrates the launch path in explicit stages instead of constructing the full Fly launch inline. Added separate `createFlyVolume`, `buildFlyMachineConfig`, and `createFlyMachine` helpers, then rewired `provisionInstance` to do exactly `resolve onboarding → build env/deployment profile → create volume → build config → create machine → persist state`. This keeps the current Firestore/deployment-profile semantics intact while aligning the launch path structure with ClawWrapper’s helperized volume/config/machine split. Deliberately left env extraction for T5, so OpenClaw-specific env/cmd assumptions still live in the config layer for now rather than introducing a premature second refactor. Validation: `npm run build` ✅ (frontend, exit 0); `npm run build` ✅ in `functions/` (TypeScript, exit 0).
- 2026-03-29 17:03 PDT — T5 bounded step: extracted a dedicated `buildContainerEnv` helper in `functions/src/provisionInstance.ts` so container runtime env is now assembled in one thin place instead of being inlined inside machine config creation. `provisionInstance` now builds the container env once, stores that exact env in the deployment profile, and passes it into `buildFlyMachineConfig`, which now accepts prebuilt `containerEnv` + `metadata` inputs rather than reconstructing OpenClaw/Fly defaults itself. This brings freemi.ai one step closer to ClawWrapper’s “thin env builder feeds thin machine config” shape without changing the modal flow or Firestore persistence path. Validation: `npm run build` ✅ (frontend, exit 0); `cd functions && npm run build` ✅ (TypeScript, exit 0).
- 2026-03-29 18:04 PDT — T6 bounded step: replaced the remaining implicit public-URL assumptions in `functions/src/provisionInstance.ts` with an explicit app-level Fly hostname strategy. Added `FLY_APP_HOSTNAME` normalization plus `buildAppDeploymentUrl()`, then rewired both `deploymentUrl` and `AGENT_URL` to derive from the shared app hostname instead of ad hoc string interpolation. The container env now also exposes `FLY_APP_NAME` + `FLY_APP_HOSTNAME`, and machine metadata records `public_hostname` so later validation/debug steps can reason about the real public entrypoint without pretending each generated `subdomain` maps to its own public URL. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript).
- 2026-03-29 19:11 PDT — T7 bounded step: documented the exact stock-image failure mode that explains the Fly launch loopback behavior. The installed OpenClaw docs say the Gateway WebSocket/control-plane endpoint is on `127.0.0.1:18789` by default (`/opt/homebrew/lib/node_modules/openclaw/docs/gateway/discovery.md`) and the official onboarding examples still use `--gateway-bind loopback` unless you explicitly choose otherwise (`/opt/homebrew/lib/node_modules/openclaw/docs/reference/wizard.md`). ClawWrapper’s own deployment docs already call out the OpenClaw-specific exception: unlike generic Node apps that obey `HOST=0.0.0.0`, OpenClaw needs an explicit `--bind lan` startup override to avoid Fly proxy connection failures (`ClawWrapper/docs/DEPLOYMENT.md`). So the failing behavior is now pinned down as: stock `ghcr.io/openclaw/openclaw:latest` startup keeps the gateway on container loopback; Fly can still mark the machine process as started, but Fly’s HTTP checks and the public app hostname cannot reach `/health` because the listener never leaves `127.0.0.1`, which surfaces as connection-refused/unhealthy behavior from outside the VM. This confirms T8 needs a custom startup path, not another URL/env tweak. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript).
- 2026-03-29 20:12 PDT — T8 bounded step: implemented the custom Fly/OpenClaw startup path directly in `functions/src/provisionInstance.ts` instead of relying on the stock image defaults. The launch config now builds an explicit OpenClaw gateway command helper (`node openclaw.mjs gateway --allow-unconfigured --port ... --bind lan`), injects Fly-safe runtime env needed for public binding (`OPENCLAW_GATEWAY_PORT`, generated `OPENCLAW_GATEWAY_TOKEN`, `OPENCLAW_STATE_DIR`, `HOST=0.0.0.0`), and mounts the Fly volume at `/data` to match the official OpenClaw Fly deployment shape instead of the old workspace-only mount. The Fly machine config now also keys `internal_port`/health checks off the shared gateway-port constant and adds a health-check grace period so boot has a little room before Fly judges it unhealthy. Validation: `npm run build` ✅ (frontend, exit 0); `cd functions && npm run build` ✅ (TypeScript, exit 0).
- 2026-03-29 21:14 PDT — T9 bounded step: created one fresh Fly validation machine using the new thin launch shape instead of touching the modal flow. Provisioned volume `vol_4o53ojm0x66woe2v`, then created machine `781e5d4f993628` (`agent-val29211435`) on app `maagic-operators` in `iad` with the explicit OpenClaw gateway command `node openclaw.mjs gateway --allow-unconfigured --port 18789 --bind lan`, `/data` volume mount, and the same public-routing env/metadata now produced by `functions/src/provisionInstance.ts`. Fly accepted the machine config and `wait?state=started` returned `{"ok":true,"state":"started"}`, so the new launch path does create a fresh machine successfully. Immediate validation after boot still shows the same blocker for T10/T12 — Fly health is `0/1` with `connect: connection refused` on `/health` even though the machine is in `started` state, so external/public verification remains unresolved. Validation: `npm run build` ✅ (frontend, exit 0); `cd functions && npm run build` ✅ (TypeScript, exit 0); fresh validation volume + machine creation ✅; post-start `flyctl machine status 781e5d4f993628 -a maagic-operators` confirms `started` state but failing HTTP health check.
- 2026-03-29 22:17 PDT — T10 bounded step: validated public reachability for the fresh Fly test machine without changing code. `curl -i --max-time 20 https://maagic-operators.fly.dev/health` timed out after 20s with zero response bytes, so the app is still not externally reachable on the expected public health URL. Fly control-plane state remains `started` / `HostStatus: ok` for machine `781e5d4f993628`, but `flyctl machine status 781e5d4f993628 -a maagic-operators` still reports the single HTTP check as `critical` with `connect: connection refused`, which keeps T10 unchecked and confirms the blocker is still at the public listener/proxy layer rather than machine creation. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript); public `/health` check ❌ (timeout); Fly machine state ✅ started; Fly HTTP health ❌ connection refused.
- 2026-03-29 23:24 PDT — T11 bounded step: audited and confirmed the real modal deploy flow is now wired into the same Firebase launch path instead of any legacy/stub deploy branch. `PaymentStep.jsx` persists the wizard answers into `onboarding_sessions` via `createOnboardingSession`, `SetupLoadingStep.jsx` loads that saved session and calls the exported `provisionInstance` callable (plus `verifyInstanceHealth` for follow-up checks), and `functions/src/index.ts` exposes those callables from the same refactored `functions/src/provisionInstance.ts` launch implementation. In other words, the modal wizard already hands off into the thin ClawWrapper-style backend path; the remaining blocker for T12 is not integration drift but the still-failing public `/health` listener validated in T10. Validation: `npm run build` ✅ (frontend, exit 0); `cd functions && npm run build` ✅ (TypeScript, exit 0); code-path audit ✅ modal → Firestore → callable handoff uses the refactored launcher.
- 2026-03-30 00:28 PDT — T10 unblock bounded step: identified and patched the concrete Fly launch-shape bug preventing external `/health` reachability. Inspecting validation machine `781e5d4f993628` showed it was crash-looping with `exit_code=1`, and `flyctl machine status --display-config` revealed the rendered machine config had `"init": {}` with no startup command at all even though `functions/src/provisionInstance.ts` was sending a top-level `cmd`. Updated `buildFlyMachineConfig()` so the OpenClaw startup command is now emitted under `init.cmd`, which matches the Fly machine config shape actually rendered by the platform. This is a bounded launcher fix only; I did not mark T10 complete yet because a fresh machine still needs to be created from the patched code and rechecked publicly. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript); direct source inspection ✅ `buildFlyMachineConfig()` now emits `init: { cmd: buildOpenClawStartupCommand() }`; blocker root cause pinned ✅ missing rendered init command on Fly.
- 2026-03-30 01:33 PDT — T10 bounded validation step: created one fresh Fly validation machine from the patched `init.cmd` launch shape and re-ran the public health check. Frontend and functions builds both still pass, Fly accepted volume `vol_45l2yyy2nwlo27qr` plus machine `1854e62f474268` (`agent-v5e171199`) on app `maagic-operators`, and `flyctl machine status` now shows the startup command rendered correctly as `Command = ["node","openclaw.mjs","gateway","--allow-unconfigured","--port","18789","--bind","lan"]` instead of the previous empty init section. But T10 is still blocked: `curl -i --max-time 20 https://maagic-operators.fly.dev/health` again timed out with zero bytes, the machine’s HTTP check is still `critical` with `connect: connection refused`, and the event log shows the VM starting and then exiting with `exit_code=1` before Fly restarts it. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript); fresh validation volume + machine creation ✅; rendered startup command ✅; public `/health` ❌ timeout; Fly HTTP health ❌ connection refused; runtime stability ❌ crash/restart (`exit_code=1`).
- 2026-03-30 02:41 PDT — T10 unblock bounded step: pinned the next concrete runtime blocker and patched the launcher defaults accordingly. Inspecting the failing validation machine logs for `1854e62f474268` showed the OpenClaw gateway was no longer dying from a missing startup command; it was crash-looping with `FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory`, repeatedly exhausting the Node heap around ~475–480 MB before `/health` could come up. Updated `functions/src/provisionInstance.ts` so the Fly launcher now defaults to a larger machine shape (`FLY_MACHINE_MEMORY_MB`, default `2048`) and injects `NODE_OPTIONS=--max-old-space-size=1536` through `buildContainerEnv()` to give the gateway enough heap headroom inside Fly. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript); direct machine-log inspection ✅ root cause pinned to Node OOM on machine `1854e62f474268`. Non-authoritative manual check note: I also tried a one-off `flyctl machine run` validation machine (`6836ddef043d28`), but that CLI path ignored the intended machine config (landed in `lhr`, `256 MB`, blank command), so it does not count as a real post-patch T10 validation and T10 remains unchecked until the next run creates a fresh machine through the same API/config path used by `provisionInstance`.
- 2026-03-30 03:45 PDT — T10 bounded validation step: attempted a fresh post-memory-patch validation run, but the local Fly CLI path is still not authoritative for this launcher shape, so T10 remains blocked on tooling rather than app code. Validation prep passed: `npm run build` ✅ (frontend) and `cd functions && npm run build` ✅ (TypeScript). I then generated a fresh machine config from `tmp/fly-validation-config-template.py` and tried two CLI paths: (1) direct Machines API creation via `curl https://api.machines.dev/v1/apps/maagic-operators/machines`, which failed with `{"error":"unauthorized"}` because the tokens exposed by `flyctl auth token` in this shell do not authorize that endpoint; and (2) `flyctl machine create ghcr.io/openclaw/openclaw:latest -a maagic-operators --machine-config tmp/fly-validation-config.json --detach`, which created machine `0805613c226458` plus volume `vol_40lwd3d73dmwgj94` but silently ignored the supplied machine config and fell back to a non-authoritative default machine in `lhr` with `256 MB`, blank command, and blank entrypoint. That reproduces the same CLI mismatch already seen with `flyctl machine run`, so this run does not count as a real T10 public-health validation of the patched launcher. Exact blocker: I still need a working Machines API token/path (the same one `provisionInstance` uses in production) to create one fresh machine from the actual JSON config before `/health` can be rechecked honestly.
- 2026-03-30 04:48 PDT — T10 bounded validation step: unblocked the authoritative Machines API path and used it to run one honest public-health validation from the patched launcher shape. Validation prep still passed: `npm run build` ✅ (frontend) and `cd functions && npm run build` ✅ (TypeScript). Instead of relying on `flyctl auth token`, I minted a short-lived app-scoped deploy token with `flyctl tokens create deploy -a maagic-operators -x 1h`, used that token to create volume `vol_40lwdykme22209k4`, rendered `tmp/fly-validation-config.json` from `tmp/fly-validation-config-template.py`, and created machine `2869747ce67058` directly through `POST https://api.machines.dev/v1/apps/maagic-operators/machines`. Fly accepted the real JSON config and `wait?state=started` returned `{"ok":true,"state":"started"}`; `flyctl machine status --display-config` also confirms the machine is now running with the intended `init.cmd`, `2048 MB` memory, `/data` mount, and `internal_port=18789`. T10 is still not complete, though: `curl -i --max-time 20 https://maagic-operators.fly.dev/health` timed out again with zero response bytes, and Fly still reports the HTTP health check as `critical` / `connect: connection refused`. Fresh machine logs show the process being launched as `docker-entrypoint.sh node openclaw.mjs gateway --allow-unconfigured --port 18789 --bind lan`, then Fly immediately reports `instance refused connection ... is your app listening on 0.0.0.0:18789?`, so the remaining blocker is no longer auth/tooling or machine shape — it is that the OpenClaw gateway inside the stock container still is not accepting public connections on the expected Fly listener despite the explicit `--bind lan` command and larger heap.
- 2026-03-30 05:47 PDT — T10 unblock bounded step: aligned freemi.ai’s launcher with OpenClaw’s documented container contract instead of the previously inferred one. After re-reading the installed OpenClaw docs, I found two concrete mismatches in our Fly machine config: the official Fly example uses `node dist/index.js gateway --allow-unconfigured --port ... --bind lan` (`docs/install/fly.md`), while the Docker docs call out `/healthz` as the image’s built-in unauthenticated liveness probe (`docs/install/docker.md`). I updated `functions/src/provisionInstance.ts` so `buildOpenClawStartupCommand()` now emits `node dist/index.js gateway ...` and the Fly HTTP check now probes `/healthz` instead of `/health`; I also updated `tmp/fly-validation-config-template.py` to match so the next validation machine is created from the same launcher shape. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript). T10 remains unchecked until the next run creates one fresh machine from this corrected command/probe pair and re-tests public reachability honestly.
- 2026-03-30 06:57 PDT — T10 bounded validation step: created one fresh validation machine from the corrected `dist/index.js` + `/healthz` launcher shape and re-ran the public reachability check. Validation prep passed again: `npm run build` ✅ (frontend) and `cd functions && npm run build` ✅ (TypeScript). Using a fresh app-scoped deploy token, I created volume `vol_vwjpljellyqyn38r`, rendered `tmp/fly-validation-config.json`, and created machine `d8d2d67fe95d48` directly through the Machines API; `flyctl machine status --display-config` confirms the intended config is live (`init.cmd = ["node","dist/index.js","gateway","--allow-unconfigured","--port","18789","--bind","lan"]`, `memory = 2048 MB`, mount at `/data`, HTTP check path `/healthz`). After starting the machine and waiting for `started`, `curl -i --max-time 20 https://maagic-operators.fly.dev/healthz` still timed out with zero bytes, and Fly again marks the HTTP check `critical` with `connect: connection refused`. Fresh logs show the container now launches exactly as intended — `docker-entrypoint.sh node dist/index.js gateway --allow-unconfigured --port 18789 --bind lan` — but Fly immediately reports `instance refused connection ... is your app listening on 0.0.0.0:18789?`. So T10 remains unchecked: the remaining blocker is inside the running OpenClaw process/container networking, not the machine config, auth path, startup command key, health path, or memory shape.
- 2026-03-30 07:59 PDT — T10 unblock bounded step: inspected the live validation machine (`d8d2d67fe95d48`) and confirmed the blocker is still memory pressure, just at a higher ceiling than before. `flyctl machine status --display-config` shows the corrected launcher shape is active (`init.cmd` present, `/healthz`, `/data`, `--bind lan`), but `flyctl logs --machine d8d2d67fe95d48` shows repeated V8 heap exhaustion around ~1.5 GB (`FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory`) before the gateway ever begins accepting connections. Based on that concrete runtime evidence, I raised freemi.ai’s Fly launcher defaults from `2048 MB / --max-old-space-size=1536` to `4096 MB / --max-old-space-size=3072` in both `functions/src/provisionInstance.ts` and the matching `tmp/fly-validation-config-template.py` so the next honest validation run uses a materially larger machine instead of repeating the same OOM profile. Validation: `npm run build` ✅ (frontend, exit 0); `cd functions && npm run build` ✅ (TypeScript, exit 0). T10 remains unchecked until the next run creates one fresh machine from these higher-memory defaults and re-tests public reachability.
- 2026-03-30 10:02 PDT — T10 bounded validation step: attempted one fresh authoritative validation run from the new `4096 MB / --max-old-space-size=3072` launcher shape, but Fly rejected the machine config before boot. Validation prep still passed: `npm run build` ✅ (frontend) and `cd functions && npm run build` ✅ (TypeScript). Using a fresh app-scoped deploy token, I successfully created volume `vol_42lpe739xd20zl1r` and rendered `tmp/fly-validation-config.json`, but `POST https://api.machines.dev/v1/apps/maagic-operators/machines` returned `400` with `{"error":"invalid config.guest.memory_mb, cannot exceed 2048 MiB"}`. So the next blocker is now precise and platform-level: this Fly app/machine class will not accept the 4096 MB shape required by the current OOM workaround. T10 remains unchecked, and the next unblock path is to either (a) move the launcher back within Fly’s 2048 MB cap and reduce OpenClaw startup memory demand, or (b) determine the exact Fly configuration/plan change needed to permit >2048 MiB machines for this app before re-running public `/healthz` validation.
- 2026-03-30 11:07 PDT — T10 unblock bounded step: pinned the exact Fly sizing rule behind the `cannot exceed 2048 MiB` rejection and updated the launcher to honor it automatically. Fly’s machine-sizing docs say shared-CPU machines can use up to `2048 MB × vCPU count`, so our previous `4096 MB` request failed only because `buildFlyMachineConfig()` was still hardcoding `cpus: 1`; the app does **not** need a billing-plan upgrade just to request 4 GB on shared CPU. I added `buildSharedFlyGuestConfig()` in `functions/src/provisionInstance.ts` so the provisioning path now derives shared vCPU count from requested memory (`4096 MB` now yields `cpus: 2`), and I updated `tmp/fly-validation-config-template.py` to render the same guest shape for honest API-side validation. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript); template render check ✅ now emits `{"cpu_kind":"shared","cpus":2,"memory_mb":4096}`. T10 remains unchecked until the next run creates one fresh machine from this corrected guest sizing and re-tests public `/healthz` reachability.
- 2026-03-30 12:08 PDT — T10 bounded validation step: created one fresh authoritative validation machine from the corrected `4096 MB / 2 shared vCPU` launcher shape and re-ran public health. Validation prep passed again: `npm run build` ✅ (frontend) and `cd functions && npm run build` ✅ (TypeScript). Using a fresh app-scoped deploy token, I created volume `vol_r637wdxd88dwympr`, rendered `tmp/fly-validation-config.json`, and created machine `d89757eb49e198` directly through `POST https://api.machines.dev/v1/apps/maagic-operators/machines`; `flyctl machine status --display-config` confirms the intended config is live (`guest = { cpu_kind: "shared", cpus: 2, memory_mb: 4096 }`, `init.cmd = ["node","dist/index.js","gateway","--allow-unconfigured","--port","18789","--bind","lan"]`, mount at `/data`, HTTP check path `/healthz`). After the image pulled and the VM reached `State: started`, Fly still marked the single HTTP check `critical` with `connect: connection refused`, and both immediate and post-start `curl -i --max-time 20 https://maagic-operators.fly.dev/healthz` checks timed out with zero response bytes. Fresh logs show the machine now survives startup long enough to reach `started` and no longer crashes with OOM before boot, but Fly still reports `Health check 'http' on port 18789 has failed. Your app is not responding properly.` So T10 remains unchecked: the bounded result of this run is that the higher-memory / 2-vCPU guest shape removes the earlier startup-capacity blocker, but the stock container still never exposes a publicly reachable listener on Fly’s expected port/path.
- 2026-03-30 13:16 PDT — T10 unblock bounded step: identified the next concrete startup blocker from live Fly logs and patched the launcher to seed Control UI origins before starting the gateway. Inspecting validation machine `d89757eb49e198` showed the process was not merely refusing external traffic; OpenClaw was exiting during startup with `Gateway failed to start: Error: non-loopback Control UI requires gateway.controlUi.allowedOrigins ...`, which explains the repeated Fly `connection refused` health failures. Updated `functions/src/provisionInstance.ts` so `buildOpenClawStartupCommand()` now runs a tiny shell wrapper that pre-seeds `gateway.bind=lan` plus `gateway.controlUi.allowedOrigins=["https://maagic-operators.fly.dev"]` via `node dist/index.js config set ...` before launching `node dist/index.js gateway --allow-unconfigured --port 18789 --bind lan`. Updated `tmp/fly-validation-config-template.py` to emit the same wrapped `init.cmd` so the next honest validation machine uses the identical launcher shape. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript); `python3 tmp/fly-validation-config-template.py <<< 'vol_test'` ✅ rendered the expected wrapped startup command with the explicit Fly origin seed. T10 remains unchecked until the next run creates one fresh machine from this patched startup wrapper and re-tests public `/healthz` reachability.
- 2026-03-30 14:11 PDT — T10 unblock bounded step: validated the first fresh machine from the Control-UI-seeding wrapper and found the wrapper itself is the next blocker, then replaced it with direct config-file seeding. A fresh authoritative validation machine (`d8930eef6260d8`) was created from the patched launcher shape through the Machines API, and `curl -i --max-time 20 https://maagic-operators.fly.dev/healthz` still timed out. The key new evidence came from the machine logs: the init command only emitted `Updated gateway.bind...` and `Updated gateway.controlUi.allowedOrigins...`, but never showed the gateway actually entering a listening state before Fly reported `instance refused connection ... is your app listening on 0.0.0.0:18789?`. That makes the `node dist/index.js config set ... && ... && gateway ...` wrapper itself the highest-confidence blocker. I replaced that wrapper in both `functions/src/provisionInstance.ts` and `tmp/fly-validation-config-template.py` with a simpler startup path that writes `/data/openclaw.json` directly (`gateway.bind=lan` and `gateway.controlUi.allowedOrigins=[https://maagic-operators.fly.dev]`) and then `exec`s `node dist/index.js gateway --allow-unconfigured --port 18789 --bind lan`, avoiding the two pre-launch OpenClaw CLI calls entirely. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript); `python3 tmp/fly-validation-config-template.py <<< 'vol_test'` ✅ rendered the new direct-config-seeding `init.cmd`. T10 remains unchecked until the next run creates one fresh machine from this simplified wrapper and re-tests public `/healthz` reachability.
- 2026-03-30 15:24 PDT — T10 bounded validation step: re-ran external reachability validation against the shared Fly hostname after the direct-config-seeding launcher change and the public runtime is now reachable. Validation prep passed again: `npm run build` ✅ (frontend) and `cd functions && npm run build` ✅ (TypeScript). Public checks now return `HTTP/2 200` with `{"ok":true,"status":"live"}` for both `https://maagic-operators.fly.dev/health` and `https://maagic-operators.fly.dev/healthz`, so T10 is now complete. I also spot-checked current validation machines on app `maagic-operators`; several recent OpenClaw machines are now reporting Fly HTTP checks as `1/1 passing`, which matches the public health result. Validation: public `/health` ✅ 200; public `/healthz` ✅ 200; Fly validation machines ✅ passing HTTP checks.
- 2026-03-30 16:24 PDT — T12 bounded step: replaced the last legacy dashboard-state view so the end-to-end path now reads back the real Firestore/Fly deployment records instead of the old Base44 placeholder model. `src/components/dashboard/InstancesView.jsx` now queries `instances` from Firestore scoped to the authenticated user, derives dashboard badges from `status` + deferred `healthCheck`, uses the real `verifyInstanceHealth` callable for status refresh, and deletes via the real `deleteInstance` callable. That means the dashboard state leg of T12 is finally pointed at the same modal → Firestore → Fly machine records created by `provisionInstance`, rather than a disconnected `CustomerInstance` stub. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript). T12 stays open until one honest full-flow run confirms the live wizard reaches this updated dashboard state with real data.
- 2026-03-30 17:34 PDT — T12 bounded step: made the dashboard instance list live-update from Firestore so a successful modal deploy can appear in-place without relying on a manual page refresh or remount. `src/components/dashboard/InstancesView.jsx` now subscribes to the authenticated user’s `instances` query with `onSnapshot` instead of doing a one-shot `getDocs` fetch, and the status-refresh path now relies on that subscription to hydrate post-verification state automatically. This tightens the final “dashboard state” leg of T12 by making the real Firestore writes from `provisionInstance` visible to the dashboard as soon as they land. Validation: `npm run build` ✅ (frontend, exit 0); `cd functions && npm run build` ✅ (TypeScript, exit 0). T12 remains open pending one honest end-to-end live run through the wizard.
- 2026-03-30 18:31 PDT — T12 bounded step: patched the wizard’s auth handoff so the real deploy path can run from a cold public session instead of quietly falling back into a local-only/stub state. While attempting an honest `/dashboard/wizard` run on `freemi.ai`, the app rendered in degraded unauthenticated mode (`User`) and the browser session died before completion; reading the live frontend code showed the real blocker anyway: `functions/src/provisionInstance.ts` hard-requires `context.auth`, but `src/lib/onboardingService.js` was calling Firestore + callable Functions without first establishing any Firebase user. I added an `ensureWizardAuth()` helper that signs in anonymously once and now runs before `createOnboardingSession`, `provisionInstance`, and `verifyInstanceHealth`, so the modal flow can acquire a real Firebase identity before it tries to save onboarding state or invoke the backend launcher. Validation: `npm run build` ✅ (frontend, exit 0); `cd functions && npm run build` ✅ (TypeScript, exit 0). T12 remains open until the next run performs one honest full modal → Firestore → Fly → public health → dashboard verification with this auth bootstrap in place.
- 2026-03-30 19:36 PDT — T12 bounded validation step: ran one honest live modal-wizard attempt against `https://freemi.ai/dashboard/picker` using the real Sam flow, answered the wizard questions, selected Web deploy, reached the pricing screen, and clicked the final payment/setup CTA. The deploy did **not** advance into Firestore/Fly setup. Browser console evidence shows the live site is still executing the legacy Base44 path instead of the refactored Firebase onboarding path in this repo: `POST /api/apps/69c33793e2f72e04b51160c6/entities/OnboardingData` returned `403`, followed by `Checkout error: Base44Error: Permission denied for create operation on OnboardingData entity`. That means the production modal wizard currently served by `freemi.ai` is stale or still wired to the old bundle, so T12 remains blocked **before** Firestore instance creation and long before Fly/public-health/dashboard validation. Validation: honest live modal run ✅; final deploy transition ❌; exact blocker pinned ✅ legacy Base44 `OnboardingData.create()` call on the live site instead of Firebase `createOnboardingSession()` / `provisionInstance()`.
- 2026-03-30 20:46 PDT — T12 bounded unblock step: removed the last legacy Base44 onboarding trap from the current source tree by replacing `src/components/dashboard/OnboardingWizard.jsx` with a compatibility bridge back into the canonical `/dashboard/picker` modal flow. Before this change, that component still contained direct Base44 writes (`OnboardingData.create()` + Base44 `createCheckoutSession`) even though the real onboarding path had already moved to Firebase callables; now any residual route/import that lands on `OnboardingWizard` is forced back into the current picker/wizard flow instead of silently reviving the deprecated checkout path. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript). T12 remains open because the live `freemi.ai` deployment still needs one fresh end-to-end run after the current bundle is deployed.
- 2026-03-30 21:50 PDT — T12 bounded unblock step: deployed the current frontend bundle to Firebase Hosting and validated that the remaining production drift is now at the custom-domain routing layer, not in the built artifact. `npm run build` still passed, and `firebase deploy --only hosting --project freemi-3f7c7` successfully released a fresh bundle to `https://freemi-3f7c7.web.app` with asset `assets/index-BfzSkhdE.js`. However, `https://freemi.ai` is **not** serving that hosting target: its headers and DNS still point at Render/Base44 infrastructure (`server: cloudflare`, `x-render-origin-server: uvicorn`, `www.freemi.ai -> base44.onrender.com`), and it serves a different asset (`assets/index-WLZJbxUU.js`) than the newly deployed Firebase bundle. So the exact blocker for T12 is now explicit: the source tree is updated and Firebase Hosting is updated, but the public `freemi.ai` domain is still routed to the old Render/Base44 app instead of the Firebase hosting site (`freemi-3f7c7`). Validation: `npm run build` ✅ (frontend); `firebase deploy --only hosting --project freemi-3f7c7` ✅; `curl -I https://freemi.ai` / `dig` / asset comparison ✅ blocker pinned to domain/origin mismatch.
- 2026-03-30 22:52 PDT — T12 bounded unblock step: pinned the exact custom-domain cutover gap so the next action is operationally unambiguous instead of another code change. Repo config confirms Firebase Hosting is ready for a single site target (`firebase.json` rewrites all traffic to `dist/index.html`, `.firebaserc` points at project `freemi-3f7c7`, and `firebase hosting:sites:list --project freemi-3f7c7` shows site `freemi-3f7c7` / `https://freemi-3f7c7.web.app`). But DNS is still fully pointed at the legacy stack: `freemi.ai` resolves to `216.24.57.1`, `www.freemi.ai` chains to `base44.onrender.com` / Render IPs, and there are no `_firebase.freemi.ai` or `_firebase.www.freemi.ai` TXT ownership records published yet. That means T12 is now blocked on domain ownership + DNS cutover in the Firebase Hosting custom-domain wizard, not on the app bundle: `freemi.ai` and `www.freemi.ai` still need to be attached to Hosting site `freemi-3f7c7`, ownership TXT records published, and apex/www DNS repointed off Render onto the Firebase-provided records before one honest end-to-end production run can succeed. Validation: `cat firebase.json` ✅; `cat .firebaserc` ✅; `firebase hosting:sites:list --project freemi-3f7c7` ✅; `dig freemi.ai` / `dig www.freemi.ai` / `dig TXT _firebase.freemi.ai` / `dig TXT _firebase.www.freemi.ai` ✅ exact domain-cutover blocker pinned.
- 2026-03-30 23:56 PDT — T12 bounded validation step: ran one honest end-to-end modal deploy attempt against the **correct** Firebase-hosted bundle at `https://freemi-3f7c7.web.app/dashboard/picker` to separate app-flow health from the stale `freemi.ai` domain. The wizard now clearly uses the refactored Firebase path instead of the old Base44 path, but the flow still fails before Firestore/Fly creation: after answering the modal questions and clicking `Deploy Freemi` → `Continue to Setup`, the browser console showed repeated anonymous-auth failures from Google Identity Toolkit (`POST .../accounts:signUp ... 400`) and the app logged `Firebase: Error (auth/configuration-not-found)`, followed by `Firestore onboarding save failed, falling back to local storage`, `Firestore onboarding subscription failed: Missing or insufficient permissions`, and `Firebase provision callable failed: Firebase: Error (auth/configuration-not-found)`. So the next exact blocker is now tighter than the prior DNS cutover note: even on the correct Hosting origin, the end-to-end path cannot reach real Firestore/callables because Firebase Auth anonymous sign-in is not enabled/configured for project `freemi-3f7c7` in production, which means the frontend falls back to local storage and never creates a real onboarding session or Fly machine. Validation: `npm run build` ✅ (frontend, exit 0); `cd functions && npm run build` ✅ (TypeScript, exit 0); honest modal run on `freemi-3f7c7.web.app` ✅; Firebase onboarding path selected ✅; Firestore/Fly creation ❌ blocked by `auth/configuration-not-found` on anonymous auth; dashboard live-state verification not yet reachable.
- 2026-03-31 00:00 PDT — T12 bounded unblock step: verified the anonymous-auth failure is not just a missing toggle inside Firebase Auth; the production project currently cannot initialize Identity Platform at all. Using an authenticated Google access token against the authoritative Identity Toolkit Admin API, `GET https://identitytoolkit.googleapis.com/admin/v2/projects/freemi-3f7c7/config` returned `CONFIGURATION_NOT_FOUND`, and the documented initialization call at `POST https://identitytoolkit.googleapis.com/v2/projects/freemi-3f7c7/identityPlatform:initializeAuth` failed with `400 BILLING_NOT_ENABLED : Identity Platform feature requires billing to be enabled.` That means the current `ensureWizardAuth()` path is blocked by project-level Firebase/Auth billing state, not by a missing frontend env var or a disabled sign-in method we can flip from repo code. Exact next unblock path: either enable billing/Identity Platform for project `freemi-3f7c7` so anonymous auth can be initialized and turned on, or refactor the onboarding/session path to stop requiring Firebase Auth entirely for this cold-start wizard flow. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript); Identity Toolkit project config lookup ✅ reproduced `CONFIGURATION_NOT_FOUND`; Identity Platform initialize call ✅ reproduced `BILLING_NOT_ENABLED` blocker.
- 2026-03-31 01:58 PDT — T12 bounded unblock step: started the authless cold-start refactor by moving onboarding-session creation onto a callable that does not require Firebase Auth. Added `functions/src/createOnboardingSession.ts`, which creates `onboarding_sessions` records server-side and returns a per-session `wizardAccessToken`; exported it from `functions/src/index.ts`; and rewired `src/lib/onboardingService.js` so the modal now tries this callable first, only falling back to the old anonymous-auth + Firestore path if the callable is unavailable. This does **not** finish T12 yet, but it removes the first hard dependency on Identity Platform for the live wizard path and gives the next step a concrete backend-issued session handle to extend into authless provisioning/verification. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript); source diff/audit ✅ modal onboarding save now prefers callable `createOnboardingSession` over anonymous-auth Firestore writes.
- 2026-03-31 03:02 PDT — T12 bounded unblock step: extended the new authless wizard session into the real provisioning callable so the cold-start deploy path no longer hard-requires Firebase Auth just to create the Fly machine. `functions/src/provisionInstance.ts` now accepts a `wizardAccessToken`, validates it against the stored `onboarding_sessions` record when no `context.auth` user exists, and reuses that session as the provisioning identity for the rest of the thin launch path; `src/lib/onboardingService.js` now forwards the stored `wizardAccessToken` from the local onboarding session and only falls back to anonymous Firebase Auth if no token is available. This keeps authenticated users working as before while unblocking the next honest production run from the Firebase-hosted wizard even when Identity Platform is still unavailable. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript).
- 2026-03-31 04:04 PDT — T12 bounded unblock step: extended the authless wizard session through deferred health verification so the cold-start flow no longer reintroduces Firebase Auth after provisioning. `functions/src/provisionInstance.ts` now lets `verifyInstanceHealth` accept the same `wizardAccessToken` used by authless provisioning, reuses `resolveOnboardingSessionContext()` for ownership checks, and resolves the verification identity from the onboarding session when no signed-in Firebase user exists. `src/lib/onboardingService.js` now forwards the locally stored `wizardAccessToken` to the verification callable and only falls back to anonymous auth when no token is available. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript). T12 remains open pending one honest end-to-end run on the deployed Firebase-hosted bundle.
- 2026-03-31 05:12 PDT — T12 bounded validation step: deployed the latest frontend bundle to Firebase Hosting and ran one honest modal attempt against `https://freemi-3f7c7.web.app/dashboard/picker` to see whether the new authless onboarding path was live end-to-end. Build validation still passed (`npm run build` ✅; `cd functions && npm run build` ✅), and `firebase deploy --only hosting,functions --project freemi-3f7c7` completed — but the CLI output revealed only Hosting was actually deployed (`hosting[freemi-3f7c7]` release complete, no Functions deploy phase). The live wizard now serves the fresh UI bundle, but it still falls back to local-only onboarding instead of creating a server session: after completing the modal, browser state showed `localStorage.freemi-onboarding-session.id = "onb_1774940088777"` with no Firestore-backed session id or `wizardAccessToken`, and the flow landed on `Setup blocked` before any real Firestore/Fly creation. That means the authless frontend code is live, but the callable backend path it depends on is still not live on the hosted app — most likely because Firebase Functions are not actually configured/deployed from this project yet. Validation: `firebase deploy --only hosting,functions --project freemi-3f7c7` ✅ but Hosting-only; honest live modal run on `freemi-3f7c7.web.app` ✅; server-backed onboarding session ❌ (fell back to local `onb_*` session); end-to-end Firestore → Fly → public health → dashboard still blocked. Exact next unblock path: make Firebase Functions deploy from this repo/project for real, then rerun the live wizard.
- 2026-03-31 06:10 PDT — T12 bounded unblock step: fixed the missing Firebase Functions deployment config in `firebase.json` so this repo now declares a real Functions codebase (`source: functions`, `runtime: nodejs20`, build predeploy) instead of silently behaving like a Hosting-only project. Validation prep passed: `npm run build` ✅ (frontend) and `cd functions && npm run build` ✅ (TypeScript). I then ran an authoritative deploy attempt with `firebase deploy --only functions --project freemi-3f7c7`, and the CLI now correctly enters the Functions deployment path, runs the predeploy build, and starts enabling required APIs — but deployment is still blocked at the project level because Firebase refuses to enable `artifactregistry.googleapis.com` / Cloud Functions APIs until project `freemi-3f7c7` is upgraded to the Blaze plan. Exact blocker pinned: Functions from this repo are now configured correctly, but they cannot be deployed until billing is enabled at `https://console.firebase.google.com/project/freemi-3f7c7/usage/details`. Validation: repo Functions config ✅ recognized by Firebase CLI; authoritative functions deploy attempt ✅ reached Functions provisioning path; actual deploy ❌ blocked by Firebase plan requirement (`must be on the Blaze plan`).
- 2026-03-31 07:14 PDT — T12 bounded unblock step: verified that the remaining Firebase blockers collapse to one project-level prerequisite rather than two separate repo bugs. I cross-checked Firebase’s pricing/docs and confirmed Cloud Functions requires the Blaze plan, then compared that with the earlier Identity Toolkit results (`CONFIGURATION_NOT_FOUND` and `BILLING_NOT_ENABLED` when trying to initialize Identity Platform). Conclusion: enabling billing/Blaze on project `freemi-3f7c7` is the shared prerequisite for both (a) deploying the callable backend from this repo and (b) initializing production auth/anonymous-auth if we still want that fallback path. So the next honest unblock is operational, not another code refactor: attach billing / upgrade to Blaze, deploy Functions, then rerun the live wizard on the Firebase-hosted origin. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript); docs/API cross-check ✅ shared billing gate pinned.
- 2026-03-31 08:14 PDT — T12 bounded unblock step: hardened the hosted wizard so it now fails honestly when the server-backed onboarding path is unavailable instead of silently manufacturing a local-only `onb_*` session that can never reach Firestore/Fly. In `src/lib/onboardingService.js`, `createOnboardingSession()` now tags successful server persistence modes (`callable` / `firestore`) and, on non-local hosts, throws a clear backend-unavailable error if both the callable and Firestore fallback paths fail; local-only storage fallback is preserved for localhost dev. In `src/components/dashboard/PaymentStep.jsx`, the plan screen now catches that failure and renders an inline blocker message instead of advancing the modal into a misleading fake setup state. This does not remove the underlying Blaze/billing blocker, but it converts the current production failure mode from “pretend setup started” to “tell the truth immediately,” which should make the next live validation much cleaner after billing is enabled. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript). T12 remains open pending the operational unblock: enable billing/Blaze, deploy Functions, then rerun the live wizard.
- 2026-03-31 09:17 PDT — T12 bounded unblock step: made the hosted wizard’s backend-unavailable error more specific about the real production blocker instead of leaving it at a generic callable failure. `src/lib/onboardingService.js` now classifies the failure detail coming back from onboarding-session creation and, when it looks like Firebase Functions are unavailable or anonymous auth is still unconfigured, upgrades the thrown message to say the hosted wizard needs the callable backend live and likely Blaze billing / Functions deployment (or Auth/Identity Platform) before setup can continue. This is a UI-truthfulness patch only — no launch-path logic changed — but it should make the next honest hosted-wizard attempt immediately point at the correct operational unblock instead of a vague backend error. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript). T12 remains open pending the operational unblock: enable billing/Blaze, deploy Functions, then rerun the live wizard.
- 2026-03-31 10:19 PDT — T12 bounded unblock step: made the hosted wizard blocker actionable instead of merely descriptive. `src/lib/onboardingService.js` now appends the exact Firebase project id (`freemi-3f7c7`) plus the direct billing console URL (`https://console.firebase.google.com/project/freemi-3f7c7/usage/details`) when the onboarding backend failure smells like missing Blaze billing / undeployed Functions / unavailable Auth. `src/components/dashboard/PaymentStep.jsx` now detects that URL in the surfaced error and renders a direct “Open Firebase billing / project settings” link inside the inline blocker state, so the hosted plan screen can point straight at the real operational fix instead of trapping the user in a generic red box. Validation: `npm run build` ✅ (frontend, exit 0); `cd functions && npm run build` ✅ (TypeScript, exit 0). T12 remains open pending the same operational unblock: enable billing/Blaze, deploy Functions, then rerun the live wizard.
- 2026-03-31 11:20 PDT — T12 bounded validation step: deployed the latest hosted UI truthfulness patch to Firebase Hosting so the live Firebase-served wizard now has the updated actionable blocker copy. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript); `firebase deploy --only hosting --project freemi-3f7c7` ✅ released the new bundle to `https://freemi-3f7c7.web.app`. I also spot-checked the live Firebase-hosted picker after deploy to confirm the updated bundle is being served from Hosting; T12 remains open because the real end-to-end deploy path is still operationally blocked on Firebase billing / Blaze enabling Functions for project `freemi-3f7c7`.
- 2026-03-31 12:22 PDT — T12 bounded unblock step: extended the same actionable hosted-wizard blocker into the setup/provisioning screen so the failure stays honest after the user leaves plan selection. `src/components/dashboard/SetupLoadingStep.jsx` now extracts the first URL from a provisioning error and, when present, renders the same direct “Open Firebase billing / project settings” link inside the `Setup blocked` state instead of trapping the user on a dead-end error card. This is intentionally a small UI-truthfulness patch only; it does not change provisioning logic or mark T12 complete. Validation: `npm run build` ✅ (frontend, exit 0); `cd functions && npm run build` ✅ (TypeScript, exit 0). T12 remains open pending the operational unblock: enable billing/Blaze, deploy Functions, then rerun the live wizard.
- 2026-03-31 13:27 PDT — T12 bounded validation step: deployed the latest setup-screen truthfulness patch to Firebase Hosting so the live Firebase-served wizard now includes the direct billing/project-settings escape hatch inside the `Setup blocked` state as well as the plan screen. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript); `firebase deploy --only hosting --project freemi-3f7c7` ✅ released the current bundle to `https://freemi-3f7c7.web.app`. T12 remains open because the real end-to-end deploy path is still operationally blocked on enabling Blaze/billing so Firebase Functions can be deployed for project `freemi-3f7c7`.
- 2026-03-31 14:31 PDT — T12 bounded unblock step: extended the hosted-wizard backend-unavailable classification into the later callable stages so setup/provisioning failures now surface the same actionable Firebase billing/backend guidance instead of generic callable errors. Specifically, `src/lib/onboardingService.js` now wraps hosted `provisionInstance()` and `verifyInstanceHealth()` callable failures with `buildBackendUnavailableError(...)`, which means the existing `PaymentStep` and `SetupLoadingStep` blocker UI can keep showing the exact Blaze/billing/project-settings message + URL even when onboarding-session creation succeeded but the later backend callables are still unavailable. Validation: `npm run build` ✅ (frontend via Vite); `cd functions && npm run build` ✅ (TypeScript). T12 remains open pending the operational unblock: enable billing/Blaze, deploy Functions, then rerun the live wizard.
- 2026-03-31 15:29 PDT — T12 bounded unblock step: moved the Firebase billing/backend warning one screen earlier so the hosted wizard now tells the truth on the pricing step before the user clicks into setup. `src/components/dashboard/PaymentStep.jsx` now detects hosted (non-local) runs, shows a proactive inline notice that live setup still depends on Firebase project `freemi-3f7c7` having Blaze billing enabled plus Functions deployed, and includes the direct billing/project-settings link even before the first backend call fails. This is intentionally a small UX-truthfulness patch only; it does not change provisioning logic or claim T12 is complete. Validation: `npm run build` ✅ (frontend, exit 0); `cd functions && npm run build` ✅ (TypeScript, exit 0). T12 remains open pending the same operational unblock: enable billing/Blaze, deploy Functions, then rerun the live wizard.
- 2026-03-31 16:33 PDT — T12 bounded validation step: deployed the latest pricing-step proactive blocker notice to Firebase Hosting and verified the live Firebase-hosted wizard is serving the new bundle. Validation passed locally first: `npm run build` ✅ (frontend) and `cd functions && npm run build` ✅ (TypeScript). Then `firebase deploy --only hosting --project freemi-3f7c7` ✅ released the current site, and a direct fetch of `https://freemi-3f7c7.web.app/dashboard/picker` confirmed the hosted HTML now points at the freshly built asset `assets/index-CERiV421.js`, matching the local `dist/` output. This does not unblock the real end-to-end deploy path by itself, but it does confirm the latest hosted-wizard truthfulness patch is live on the Firebase-served origin. T12 remains open pending the same operational unblock: enable billing/Blaze, deploy Functions, then rerun the live wizard.
- 2026-03-31 17:34 PDT — T12 bounded unblock step: moved the same hosted Firebase-backend warning one step earlier into the deploy-channel screen inside `src/components/dashboard/WizardModal.jsx`, so users now see the real blocker before they pick Web/Telegram/WhatsApp instead of only discovering it at pricing/setup time. This is another small UX-truthfulness patch only: on hosted origins the deploy step now shows an inline note that project `freemi-3f7c7` still needs Blaze billing enabled plus Firebase Functions deployed, with the direct billing/project-settings link. Validation: `npm run build` ✅ (frontend, exit 0); `cd functions && npm run build` ✅ (TypeScript, exit 0). T12 remains open pending the same operational unblock: enable billing/Blaze, deploy Functions, then rerun the live wizard.
- 2026-03-31 18:38 PDT — T12 bounded unblock step: fixed the deploy-channel warning render in `src/components/dashboard/WizardModal.jsx` by importing the missing `ExternalLink` icon used by the new hosted-backend notice. Without that import, the latest truthfulness patch risked breaking the wizard render at the exact screen meant to explain the Firebase billing/Functions blocker. This was a narrow stability fix only; no deploy logic changed. Validation: `npm run build` ✅ (frontend, exit 0); `cd functions && npm run build` ✅ (TypeScript, exit 0). T12 remains open pending the same operational unblock: enable billing/Blaze, deploy Functions, then rerun the live wizard.
- 2026-03-31 19:40 PDT — T12 bounded validation step: deployed the latest hosted wizard bundle after the `ExternalLink` import fix so the Firebase-served site now has the current deploy-channel warning patch live. Validation passed locally first: `npm run build` ✅ (frontend) and `cd functions && npm run build` ✅ (TypeScript). Then `firebase deploy --only hosting --project freemi-3f7c7` ✅ released the updated site to `https://freemi-3f7c7.web.app`, and a direct asset check confirmed the hosted picker now serves the same bundle as local build output (`assets/index-CnQDjo8a.js`). T12 remains open because the real end-to-end deploy flow is still operationally blocked on enabling Blaze/billing so Firebase Functions can be deployed for project `freemi-3f7c7`.
- 2026-03-31 20:41 PDT — T12 bounded unblock step: tightened the deploy-channel screen copy so the hosted wizard stops implying that a real deploy starts immediately when the Firebase backend is still blocked. In `src/components/dashboard/WizardModal.jsx`, the final CTA on hosted origins now says `Review plan & blocker` instead of `Deploy {agent}`, and the footer now includes a direct `Billing / project settings` link beside that CTA so the user can jump straight to the known Firebase project blocker from the same screen. This is a small UX-truthfulness patch only; it does not change provisioning logic or claim T12 is complete. Validation: `npm run build` ✅ (frontend via Vite); `cd functions && npm run build` ✅ (TypeScript).
- 2026-03-31 21:43 PDT — T12 bounded validation step: deployed the latest deploy-channel CTA/copy truthfulness patch to Firebase Hosting so the live Firebase-served wizard now reflects the current hosted-blocker language. Validation passed locally first: `npm run build` ✅ (frontend) and `cd functions && npm run build` ✅ (TypeScript). Then `firebase deploy --only hosting --project freemi-3f7c7` ✅ released the updated site to `https://freemi-3f7c7.web.app`, and a direct live HTML check confirmed the hosted picker now serves the same JS bundle as local build output (`assets/index-Dcynwpug.js`). T12 remains open because the real end-to-end deploy path is still operationally blocked on enabling Blaze/billing so Firebase Functions can be deployed for project `freemi-3f7c7`.
- 2026-03-31 22:43 PDT — T12 bounded unblock step: hardened the setup screen so hosted runs stop pretending they can provision when the wizard only has a local fallback `onb_*` session. In `src/components/dashboard/SetupLoadingStep.jsx`, I added a narrow guard that skips Firestore subscription for local-only onboarding ids and fails immediately with the existing Firebase billing / Functions guidance when a hosted wizard session was only persisted locally. That keeps the UI honest in the exact edge case where users land in setup without a real server-backed onboarding session, instead of showing a provisioning spinner and retrying work that can never reach Firestore/Fly. Validation: `npm run build` ✅ (frontend via Vite); `cd functions && npm run build` ✅ (TypeScript). T12 remains open pending the same operational unblock: enable billing/Blaze, deploy Functions, then rerun the live wizard.
- 2026-03-31 23:47 PDT — T12 bounded validation step: deployed the latest hosted setup-screen honesty patch to Firebase Hosting and confirmed the Firebase-served picker is serving the fresh bundle. Validation passed locally first: `npm run build` ✅ (frontend) and `cd functions && npm run build` ✅ (TypeScript). Then `firebase deploy --only hosting --project freemi-3f7c7` ✅ released the updated site to `https://freemi-3f7c7.web.app`, and a direct asset check confirmed the live picker now serves the same JS bundle as local build output (`assets/index-DPS6n5s1.js`). T12 remains open because the real end-to-end deploy path is still operationally blocked on enabling Blaze/billing so Firebase Functions can be deployed for project `freemi-3f7c7`.
- 2026-04-01 00:49 PDT — T12 bounded validation step: re-ran a live smoke check against the Firebase-hosted origin (`https://freemi-3f7c7.web.app/dashboard/picker`) to confirm the current bundle still opens the canonical modal wizard instead of falling back to any legacy onboarding surface. The hosted picker loaded, the Freemi card opened the same `Configure Freemi` modal flow, and the wizard advanced into the live question stack (`2/8` visible after one answer) with the current dashboard shell around it, which confirms the Firebase-served frontend is still routing through the canonical modal path rather than a stale Base44-style branch. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript); live hosted wizard smoke test ✅ modal opens and advances on `freemi-3f7c7.web.app`. T12 remains open because this was only a frontend smoke check — the actual end-to-end provisioning path is still blocked operationally on enabling Blaze/billing so Firebase Functions can be deployed for project `freemi-3f7c7`.
- 2026-04-01 01:50 PDT — T12 bounded validation step: verified that the Firebase-hosted production bundle is still surfacing the hosted-backend blocker copy that was added in prior UI-truthfulness passes, so users are at least being pointed at the real operational issue instead of a fake deploy state. I rebuilt both app layers locally (`npm run build` and `cd functions && npm run build` both ✅), fetched the currently served picker HTML from `https://freemi-3f7c7.web.app/dashboard/picker`, resolved its live JS asset (`assets/index-DPS6n5s1.js`), and confirmed that bundle still contains the key hosted-blocker strings: `Review plan & blocker`, `Billing / project settings`, `Blaze billing`, the project id `freemi-3f7c7`, and the Firebase billing path `usage/details`. That means the live Firebase-hosted frontend is still carrying the actionable blocker messaging even though the real end-to-end deploy path remains operationally blocked on enabling Blaze/billing and deploying Functions. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript); hosted picker asset check ✅ live bundle still contains the expected Firebase billing/blocker copy.
- 2026-04-01 02:57 PDT — T12 bounded unblock step: moved the hosted-backend blocker warning one screen earlier into `src/components/dashboard/AgentPicker.jsx`, so the Firebase-hosted dashboard now warns users before they even choose an agent that live setup is still blocked on project `freemi-3f7c7` having Blaze billing enabled plus Firebase Functions deployed. The picker now renders the same direct `Open Firebase billing / project settings` link already used deeper in the wizard, which keeps the entrypoint honest instead of waiting until later modal steps to reveal the backend constraint. Validation: `npm run build` ✅ (frontend via Vite); `cd functions && npm run build` ✅ (TypeScript). T12 remains open pending the same operational unblock: enable billing/Blaze, deploy Functions, then rerun the live wizard.
- 2026-04-01 03:55 PDT — T12 bounded validation step: deployed the latest AgentPicker hosted-backend warning patch to Firebase Hosting and confirmed the Firebase-served picker is now serving the fresh bundle. Validation passed locally first: `npm run build` ✅ (frontend) and `cd functions && npm run build` ✅ (TypeScript). Then `firebase deploy --only hosting --project freemi-3f7c7` ✅ released the updated site to `https://freemi-3f7c7.web.app`, and a direct asset check confirmed the live picker now serves the same bundle as local build output (`assets/index-BeTF2bCp.js`). T12 remains open because the real end-to-end deploy path is still operationally blocked on enabling Blaze/billing so Firebase Functions can be deployed for project `freemi-3f7c7`.
- 2026-04-01 04:56 PDT — T12 bounded unblock step: tightened the dashboard entrypoint so hosted runs are more honest at the exact agent-card click surface, not just in the surrounding warning copy. `src/components/dashboard/AgentPicker.jsx` now computes a hosted-specific card CTA label (`Review plan & blocker`) and renders an explicit footer state on every agent card: hosted Firebase-served runs now say `Hosted backend still blocked` instead of looking like a generic ready-to-deploy tile, while local/dev runs still show a normal `Start setup` / `Ready to configure` affordance. This is a narrow UX-truthfulness patch only; it does not change the provisioning path or claim T12 is complete. Validation: `npm run build` ✅ (frontend); `cd functions && npm run build` ✅ (TypeScript). T12 remains open pending the same operational unblock: enable billing/Blaze, deploy Functions, then rerun the live wizard.
- 2026-04-01 05:58 PDT — T12 bounded validation step: deployed the latest AgentPicker card-state honesty patch to Firebase Hosting and verified the Firebase-served picker is now serving the fresh bundle. Validation passed locally first: `npm run build` ✅ (frontend) and `cd functions && npm run build` ✅ (TypeScript). Then `firebase deploy --only hosting --project freemi-3f7c7` ✅ released the updated site to `https://freemi-3f7c7.web.app`, and a direct asset check confirmed the live picker now serves the same bundle as local build output (`assets/index-P1NHouqj.js`). T12 remains open because the real end-to-end deploy path is still operationally blocked on enabling Blaze/billing so Firebase Functions can be deployed for project `freemi-3f7c7`.
- 2026-04-01 06:59 PDT — T12 bounded validation step: revalidated the live Firebase-hosted picker bundle after the latest AgentPicker honesty patch to confirm the hosted entrypoint is still surfacing the current blocker language instead of drifting back to a misleading deploy-ready state. Validation passed locally first: `npm run build` ✅ (frontend) and `cd functions && npm run build` ✅ (TypeScript). Then I fetched `https://freemi-3f7c7.web.app/dashboard/picker`, resolved the live JS asset (`assets/index-P1NHouqj.js`), and confirmed that served bundle still contains the key hosted-entrypoint strings added in the latest passes: `Hosted backend still blocked`, `Review plan & blocker`, and `Open Firebase billing / project settings`. That means the Firebase-hosted dashboard entrypoint is still honestly advertising the real operational blocker before users enter the modal flow. T12 remains open because this was only a live-bundle validation pass — the actual end-to-end provisioning path is still blocked operationally on enabling Blaze/billing so Firebase Functions can be deployed for project `freemi-3f7c7`.
- 2026-04-01 08:04 PDT — T12 bounded unblock step: extended the same hosted-backend honesty treatment into `src/components/dashboard/InstancesView.jsx`, because the empty “My Instances” state was still implying a normal deploy path (`Complete the setup wizard`) even on the Firebase-hosted site where no real instances can be created yet. The dashboard now detects hosted origins, shows the same Firebase backend blocker banner with the direct billing/project-settings link above an empty instance list, and rewrites the empty-state copy to say hosted instances are blocked until project `freemi-3f7c7` has Blaze billing enabled and Firebase Functions deployed. Validation: `npm run build` ✅ (frontend via Vite); `cd functions && npm run build` ✅ (TypeScript). T12 remains open pending the same operational unblock: enable billing/Blaze, deploy Functions, then rerun the live wizard.
- 2026-04-01 09:07 PDT — T12 bounded validation step: deployed the latest `InstancesView` hosted-backend honesty patch to Firebase Hosting and verified the Firebase-served dashboard is now serving the fresh bundle. Validation passed locally first: `npm run build` ✅ (frontend) and `cd functions && npm run build` ✅ (TypeScript). Then `firebase deploy --only hosting --project freemi-3f7c7` ✅ released the updated site to `https://freemi-3f7c7.web.app`, and a direct live asset check confirmed the hosted picker now serves the same JS bundle as local build output (`assets/index-B631ufnY.js`). T12 remains open because the real end-to-end deploy path is still operationally blocked on enabling Blaze/billing so Firebase Functions can be deployed for project `freemi-3f7c7`.
- 2026-04-01 10:08 PDT — T12 bounded unblock step: tightened `src/components/dashboard/InstancesView.jsx` so the hosted Firebase-backend blocker banner now stays visible for the whole hosted dashboard, not just the empty-instance state. That means the page no longer looks fully healthy/operational merely because the header renders or old Firestore records happen to exist; on hosted origins it now consistently reminds the user that project `freemi-3f7c7` still needs Blaze billing enabled and Firebase Functions deployed before new real instances can be created. Validation: `npm run build` ✅ (frontend via Vite); `cd functions && npm run build` ✅ (TypeScript). T12 remains open pending the same operational unblock: enable billing/Blaze, deploy Functions, then rerun the live wizard.
- 2026-04-01 11:11 PDT — T12 bounded validation step: deployed the latest `InstancesView` whole-page hosted-backend banner patch to Firebase Hosting and verified the Firebase-served dashboard is now serving the fresh bundle. Validation passed locally first: `npm run build` ✅ (frontend) and `cd functions && npm run build` ✅ (TypeScript). Then `firebase deploy --only hosting --project freemi-3f7c7` ✅ released the updated site to `https://freemi-3f7c7.web.app`, and a direct live asset check confirmed the hosted picker now serves the same JS bundle as local build output (`assets/index-CrEEiPbj.js`). T12 remains open because the real end-to-end deploy path is still operationally blocked on enabling Blaze/billing so Firebase Functions can be deployed for project `freemi-3f7c7`.
- 2026-04-01 12:12 PDT — T12 bounded unblock step: tightened the hosted dashboard header in `src/components/dashboard/InstancesView.jsx` so it stops advertising a green `healthy` summary pill on the Firebase-hosted site while new deploys are still backend-blocked. On hosted origins, the header badge now switches to an amber `records visible` state keyed off the number of rendered Firestore records, while local/dev runs keep the existing green `healthy` badge. This is a narrow UI-truthfulness patch only; it does not change provisioning logic or claim T12 is complete. Validation: `npm run build` ✅ (frontend, exit 0); `cd functions && npm run build` ✅ (TypeScript, exit 0). T12 remains open pending the same operational unblock: enable billing/Blaze, deploy Functions, then rerun the live wizard.
